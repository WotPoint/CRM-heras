meta:
  environment: nodejs
  toolVersion: "20"
  toolchain: node

build:
  buildDir: .
  additionalCommands:
    - cd frontend && npm ci --include=dev && npm run build
    - cd backend && npm ci --include=dev && npm run build

run:
  persistentStorage:
    - /storage
  runCmd: cd /app/backend && npm start
